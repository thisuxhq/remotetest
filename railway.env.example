# Environment variables for Railway deployment

# Option 1: Single origin (will automatically handle both HTTP and HTTPS)
ORIGIN=https://remotetest-production.up.railway.app

# Option 2: Multiple allowed origins (comma-separated) - for custom domains
# ALLOWED_ORIGINS=https://your-app-name.railway.app,http://your-app-name.railway.app,https://your-custom-domain.com

# Optional: Set the port (Railway usually sets PORT automatically)
PORT=3000

# Optional: Set the host (usually 0.0.0.0 for Railway)
HOST=0.0.0.0
